#include <stdio.h>
/**
 * main - Program that prints digit number of base 10 starting from 0
 *
 * Return: Always  0.
 */
int main(void)
{

	int m;

	for (m = 0; m < 10; m++)
	{
		printf("%d", m);
	}
		printf("\n");

		return (0);
}
